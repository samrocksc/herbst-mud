// Code generated by ent, DO NOT EDIT.

package db

import (
	"fmt"
	"herbst/db/activeeffect"
	"herbst/db/character"
	"herbst/db/effect"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// ActiveEffect is the model entity for the ActiveEffect schema.
type ActiveEffect struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CharacterID holds the value of the "character_id" field.
	CharacterID int `json:"character_id,omitempty"`
	// EffectID holds the value of the "effect_id" field.
	EffectID int `json:"effect_id,omitempty"`
	// AppliedByID holds the value of the "applied_by_id" field.
	AppliedByID int `json:"applied_by_id,omitempty"`
	// StackCount holds the value of the "stack_count" field.
	StackCount int `json:"stack_count,omitempty"`
	// StartedAt holds the value of the "started_at" field.
	StartedAt time.Time `json:"started_at,omitempty"`
	// ExpiresAt holds the value of the "expires_at" field.
	ExpiresAt *time.Time `json:"expires_at,omitempty"`
	// IsActive holds the value of the "is_active" field.
	IsActive bool `json:"is_active,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ActiveEffectQuery when eager-loading is set.
	Edges        ActiveEffectEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ActiveEffectEdges holds the relations/edges for other nodes in the graph.
type ActiveEffectEdges struct {
	// Character holds the value of the character edge.
	Character *Character `json:"character,omitempty"`
	// Effect holds the value of the effect edge.
	Effect *Effect `json:"effect,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// CharacterOrErr returns the Character value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ActiveEffectEdges) CharacterOrErr() (*Character, error) {
	if e.Character != nil {
		return e.Character, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: character.Label}
	}
	return nil, &NotLoadedError{edge: "character"}
}

// EffectOrErr returns the Effect value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ActiveEffectEdges) EffectOrErr() (*Effect, error) {
	if e.Effect != nil {
		return e.Effect, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: effect.Label}
	}
	return nil, &NotLoadedError{edge: "effect"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ActiveEffect) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case activeeffect.FieldIsActive:
			values[i] = new(sql.NullBool)
		case activeeffect.FieldID, activeeffect.FieldCharacterID, activeeffect.FieldEffectID, activeeffect.FieldAppliedByID, activeeffect.FieldStackCount:
			values[i] = new(sql.NullInt64)
		case activeeffect.FieldStartedAt, activeeffect.FieldExpiresAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ActiveEffect fields.
func (_m *ActiveEffect) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case activeeffect.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case activeeffect.FieldCharacterID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field character_id", values[i])
			} else if value.Valid {
				_m.CharacterID = int(value.Int64)
			}
		case activeeffect.FieldEffectID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field effect_id", values[i])
			} else if value.Valid {
				_m.EffectID = int(value.Int64)
			}
		case activeeffect.FieldAppliedByID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field applied_by_id", values[i])
			} else if value.Valid {
				_m.AppliedByID = int(value.Int64)
			}
		case activeeffect.FieldStackCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field stack_count", values[i])
			} else if value.Valid {
				_m.StackCount = int(value.Int64)
			}
		case activeeffect.FieldStartedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field started_at", values[i])
			} else if value.Valid {
				_m.StartedAt = value.Time
			}
		case activeeffect.FieldExpiresAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field expires_at", values[i])
			} else if value.Valid {
				_m.ExpiresAt = new(time.Time)
				*_m.ExpiresAt = value.Time
			}
		case activeeffect.FieldIsActive:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_active", values[i])
			} else if value.Valid {
				_m.IsActive = value.Bool
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ActiveEffect.
// This includes values selected through modifiers, order, etc.
func (_m *ActiveEffect) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryCharacter queries the "character" edge of the ActiveEffect entity.
func (_m *ActiveEffect) QueryCharacter() *CharacterQuery {
	return NewActiveEffectClient(_m.config).QueryCharacter(_m)
}

// QueryEffect queries the "effect" edge of the ActiveEffect entity.
func (_m *ActiveEffect) QueryEffect() *EffectQuery {
	return NewActiveEffectClient(_m.config).QueryEffect(_m)
}

// Update returns a builder for updating this ActiveEffect.
// Note that you need to call ActiveEffect.Unwrap() before calling this method if this ActiveEffect
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ActiveEffect) Update() *ActiveEffectUpdateOne {
	return NewActiveEffectClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ActiveEffect entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ActiveEffect) Unwrap() *ActiveEffect {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("db: ActiveEffect is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ActiveEffect) String() string {
	var builder strings.Builder
	builder.WriteString("ActiveEffect(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("character_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.CharacterID))
	builder.WriteString(", ")
	builder.WriteString("effect_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.EffectID))
	builder.WriteString(", ")
	builder.WriteString("applied_by_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.AppliedByID))
	builder.WriteString(", ")
	builder.WriteString("stack_count=")
	builder.WriteString(fmt.Sprintf("%v", _m.StackCount))
	builder.WriteString(", ")
	builder.WriteString("started_at=")
	builder.WriteString(_m.StartedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := _m.ExpiresAt; v != nil {
		builder.WriteString("expires_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("is_active=")
	builder.WriteString(fmt.Sprintf("%v", _m.IsActive))
	builder.WriteByte(')')
	return builder.String()
}

// ActiveEffects is a parsable slice of ActiveEffect.
type ActiveEffects []*ActiveEffect
