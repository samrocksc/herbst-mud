We should utilize postgres for our database.  It should be easy to set up in the makefile.

- It should use postgres
- It should use Prisma with a GoLang generator for its queries
- It should use prisma to handle the migrations
