// Code generated by ent, DO NOT EDIT.

package db

import (
	"encoding/json"
	"fmt"
	"herbst-server/db/zone"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Zone is the model entity for the Zone schema.
type Zone struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// World this zone belongs to (for multi-world support)
	WorldID string `json:"world_id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
	// MinLevel holds the value of the "min_level" field.
	MinLevel int `json:"min_level,omitempty"`
	// Parent zone for sub-zones (e.g. Old Car Garage inside Junkyard)
	ParentZoneID string `json:"parent_zone_id,omitempty"`
	// Hex color for map UI, e.g. #8b4513
	Color string `json:"color,omitempty"`
	// Explicit list of room IDs in this zone. Edge Zone.rooms is the joinable view; room_ids is the persistent membership list, including rooms that may have been removed (shown as 'ghost' / red chips in admin).
	RoomIds []int `json:"room_ids,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ZoneQuery when eager-loading is set.
	Edges        ZoneEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ZoneEdges holds the relations/edges for other nodes in the graph.
type ZoneEdges struct {
	// Parent holds the value of the parent edge.
	Parent *Zone `json:"parent,omitempty"`
	// Children holds the value of the children edge.
	Children []*Zone `json:"children,omitempty"`
	// Rooms holds the value of the rooms edge.
	Rooms []*Room `json:"rooms,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// ParentOrErr returns the Parent value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ZoneEdges) ParentOrErr() (*Zone, error) {
	if e.Parent != nil {
		return e.Parent, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: zone.Label}
	}
	return nil, &NotLoadedError{edge: "parent"}
}

// ChildrenOrErr returns the Children value or an error if the edge
// was not loaded in eager-loading.
func (e ZoneEdges) ChildrenOrErr() ([]*Zone, error) {
	if e.loadedTypes[1] {
		return e.Children, nil
	}
	return nil, &NotLoadedError{edge: "children"}
}

// RoomsOrErr returns the Rooms value or an error if the edge
// was not loaded in eager-loading.
func (e ZoneEdges) RoomsOrErr() ([]*Room, error) {
	if e.loadedTypes[2] {
		return e.Rooms, nil
	}
	return nil, &NotLoadedError{edge: "rooms"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Zone) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case zone.FieldRoomIds:
			values[i] = new([]byte)
		case zone.FieldMinLevel:
			values[i] = new(sql.NullInt64)
		case zone.FieldID, zone.FieldWorldID, zone.FieldName, zone.FieldDescription, zone.FieldParentZoneID, zone.FieldColor:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Zone fields.
func (_m *Zone) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case zone.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case zone.FieldWorldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field world_id", values[i])
			} else if value.Valid {
				_m.WorldID = value.String
			}
		case zone.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case zone.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				_m.Description = value.String
			}
		case zone.FieldMinLevel:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field min_level", values[i])
			} else if value.Valid {
				_m.MinLevel = int(value.Int64)
			}
		case zone.FieldParentZoneID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field parent_zone_id", values[i])
			} else if value.Valid {
				_m.ParentZoneID = value.String
			}
		case zone.FieldColor:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field color", values[i])
			} else if value.Valid {
				_m.Color = value.String
			}
		case zone.FieldRoomIds:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field room_ids", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.RoomIds); err != nil {
					return fmt.Errorf("unmarshal field room_ids: %w", err)
				}
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Zone.
// This includes values selected through modifiers, order, etc.
func (_m *Zone) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryParent queries the "parent" edge of the Zone entity.
func (_m *Zone) QueryParent() *ZoneQuery {
	return NewZoneClient(_m.config).QueryParent(_m)
}

// QueryChildren queries the "children" edge of the Zone entity.
func (_m *Zone) QueryChildren() *ZoneQuery {
	return NewZoneClient(_m.config).QueryChildren(_m)
}

// QueryRooms queries the "rooms" edge of the Zone entity.
func (_m *Zone) QueryRooms() *RoomQuery {
	return NewZoneClient(_m.config).QueryRooms(_m)
}

// Update returns a builder for updating this Zone.
// Note that you need to call Zone.Unwrap() before calling this method if this Zone
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Zone) Update() *ZoneUpdateOne {
	return NewZoneClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Zone entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Zone) Unwrap() *Zone {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("db: Zone is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Zone) String() string {
	var builder strings.Builder
	builder.WriteString("Zone(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("world_id=")
	builder.WriteString(_m.WorldID)
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(_m.Description)
	builder.WriteString(", ")
	builder.WriteString("min_level=")
	builder.WriteString(fmt.Sprintf("%v", _m.MinLevel))
	builder.WriteString(", ")
	builder.WriteString("parent_zone_id=")
	builder.WriteString(_m.ParentZoneID)
	builder.WriteString(", ")
	builder.WriteString("color=")
	builder.WriteString(_m.Color)
	builder.WriteString(", ")
	builder.WriteString("room_ids=")
	builder.WriteString(fmt.Sprintf("%v", _m.RoomIds))
	builder.WriteByte(')')
	return builder.String()
}

// Zones is a parsable slice of Zone.
type Zones []*Zone
