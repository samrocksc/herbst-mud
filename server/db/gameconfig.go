// Code generated by ent, DO NOT EDIT.

package db

import (
	"fmt"
	"herbst-server/db/gameconfig"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// GameConfig is the model entity for the GameConfig schema.
type GameConfig struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Config key, e.g. fountain_room_id
	Key string `json:"key,omitempty"`
	// Config value
	Value        string `json:"value,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*GameConfig) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case gameconfig.FieldID:
			values[i] = new(sql.NullInt64)
		case gameconfig.FieldKey, gameconfig.FieldValue:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the GameConfig fields.
func (_m *GameConfig) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case gameconfig.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case gameconfig.FieldKey:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field key", values[i])
			} else if value.Valid {
				_m.Key = value.String
			}
		case gameconfig.FieldValue:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field value", values[i])
			} else if value.Valid {
				_m.Value = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// GetValue returns the ent.Value that was dynamically selected and assigned to the GameConfig.
// This includes values selected through modifiers, order, etc.
func (_m *GameConfig) GetValue(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this GameConfig.
// Note that you need to call GameConfig.Unwrap() before calling this method if this GameConfig
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *GameConfig) Update() *GameConfigUpdateOne {
	return NewGameConfigClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the GameConfig entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *GameConfig) Unwrap() *GameConfig {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("db: GameConfig is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *GameConfig) String() string {
	var builder strings.Builder
	builder.WriteString("GameConfig(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("key=")
	builder.WriteString(_m.Key)
	builder.WriteString(", ")
	builder.WriteString("value=")
	builder.WriteString(_m.Value)
	builder.WriteByte(')')
	return builder.String()
}

// GameConfigs is a parsable slice of GameConfig.
type GameConfigs []*GameConfig
