// Code generated by ent, DO NOT EDIT.

package db

import (
	"context"
	"errors"
	"fmt"
	"herbst-server/db/ability"
	"herbst-server/db/npcability"
	"herbst-server/db/npctemplate"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// NPCAbilityCreate is the builder for creating a NPCAbility entity.
type NPCAbilityCreate struct {
	config
	mutation *NPCAbilityMutation
	hooks    []Hook
}

// SetSlot sets the "slot" field.
func (_c *NPCAbilityCreate) SetSlot(v int) *NPCAbilityCreate {
	_c.mutation.SetSlot(v)
	return _c
}

// AddNpcTemplateIDs adds the "npc_template" edge to the NPCTemplate entity by IDs.
func (_c *NPCAbilityCreate) AddNpcTemplateIDs(ids ...string) *NPCAbilityCreate {
	_c.mutation.AddNpcTemplateIDs(ids...)
	return _c
}

// AddNpcTemplate adds the "npc_template" edges to the NPCTemplate entity.
func (_c *NPCAbilityCreate) AddNpcTemplate(v ...*NPCTemplate) *NPCAbilityCreate {
	ids := make([]string, len(v))
	for i := range v {
		ids[i] = v[i].ID
	}
	return _c.AddNpcTemplateIDs(ids...)
}

// AddAbilityIDs adds the "ability" edge to the Ability entity by IDs.
func (_c *NPCAbilityCreate) AddAbilityIDs(ids ...int) *NPCAbilityCreate {
	_c.mutation.AddAbilityIDs(ids...)
	return _c
}

// AddAbility adds the "ability" edges to the Ability entity.
func (_c *NPCAbilityCreate) AddAbility(v ...*Ability) *NPCAbilityCreate {
	ids := make([]int, len(v))
	for i := range v {
		ids[i] = v[i].ID
	}
	return _c.AddAbilityIDs(ids...)
}

// Mutation returns the NPCAbilityMutation object of the builder.
func (_c *NPCAbilityCreate) Mutation() *NPCAbilityMutation {
	return _c.mutation
}

// Save creates the NPCAbility in the database.
func (_c *NPCAbilityCreate) Save(ctx context.Context) (*NPCAbility, error) {
	return withHooks(ctx, _c.sqlSave, _c.mutation, _c.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (_c *NPCAbilityCreate) SaveX(ctx context.Context) *NPCAbility {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *NPCAbilityCreate) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *NPCAbilityCreate) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (_c *NPCAbilityCreate) check() error {
	if _, ok := _c.mutation.Slot(); !ok {
		return &ValidationError{Name: "slot", err: errors.New(`db: missing required field "NPCAbility.slot"`)}
	}
	if len(_c.mutation.NpcTemplateIDs()) == 0 {
		return &ValidationError{Name: "npc_template", err: errors.New(`db: missing required edge "NPCAbility.npc_template"`)}
	}
	if len(_c.mutation.AbilityIDs()) == 0 {
		return &ValidationError{Name: "ability", err: errors.New(`db: missing required edge "NPCAbility.ability"`)}
	}
	return nil
}

func (_c *NPCAbilityCreate) sqlSave(ctx context.Context) (*NPCAbility, error) {
	if err := _c.check(); err != nil {
		return nil, err
	}
	_node, _spec := _c.createSpec()
	if err := sqlgraph.CreateNode(ctx, _c.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	_c.mutation.id = &_node.ID
	_c.mutation.done = true
	return _node, nil
}

func (_c *NPCAbilityCreate) createSpec() (*NPCAbility, *sqlgraph.CreateSpec) {
	var (
		_node = &NPCAbility{config: _c.config}
		_spec = sqlgraph.NewCreateSpec(npcability.Table, sqlgraph.NewFieldSpec(npcability.FieldID, field.TypeInt))
	)
	if value, ok := _c.mutation.Slot(); ok {
		_spec.SetField(npcability.FieldSlot, field.TypeInt, value)
		_node.Slot = value
	}
	if nodes := _c.mutation.NpcTemplateIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2M,
			Inverse: true,
			Table:   npcability.NpcTemplateTable,
			Columns: npcability.NpcTemplatePrimaryKey,
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(npctemplate.FieldID, field.TypeString),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := _c.mutation.AbilityIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2M,
			Inverse: true,
			Table:   npcability.AbilityTable,
			Columns: npcability.AbilityPrimaryKey,
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(ability.FieldID, field.TypeInt),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// NPCAbilityCreateBulk is the builder for creating many NPCAbility entities in bulk.
type NPCAbilityCreateBulk struct {
	config
	err      error
	builders []*NPCAbilityCreate
}

// Save creates the NPCAbility entities in the database.
func (_c *NPCAbilityCreateBulk) Save(ctx context.Context) ([]*NPCAbility, error) {
	if _c.err != nil {
		return nil, _c.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(_c.builders))
	nodes := make([]*NPCAbility, len(_c.builders))
	mutators := make([]Mutator, len(_c.builders))
	for i := range _c.builders {
		func(i int, root context.Context) {
			builder := _c.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*NPCAbilityMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, _c.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, _c.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, _c.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (_c *NPCAbilityCreateBulk) SaveX(ctx context.Context) []*NPCAbility {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *NPCAbilityCreateBulk) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *NPCAbilityCreateBulk) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}
