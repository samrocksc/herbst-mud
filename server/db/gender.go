// Code generated by ent, DO NOT EDIT.

package db

import (
	"fmt"
	"herbst-server/db/gender"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Gender is the model entity for the Gender schema.
type Gender struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Internal ID: he_him, she_her, they_them
	Name string `json:"name,omitempty"`
	// Shown in UI: He/Him, She/Her, They/Them
	DisplayName string `json:"display_name,omitempty"`
	// he, she, they
	SubjectPronoun string `json:"subject_pronoun,omitempty"`
	// him, her, them
	ObjectPronoun string `json:"object_pronoun,omitempty"`
	// his, hers, theirs
	PossessivePronoun string `json:"possessive_pronoun,omitempty"`
	selectValues      sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Gender) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case gender.FieldID:
			values[i] = new(sql.NullInt64)
		case gender.FieldName, gender.FieldDisplayName, gender.FieldSubjectPronoun, gender.FieldObjectPronoun, gender.FieldPossessivePronoun:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Gender fields.
func (_m *Gender) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case gender.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case gender.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case gender.FieldDisplayName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field display_name", values[i])
			} else if value.Valid {
				_m.DisplayName = value.String
			}
		case gender.FieldSubjectPronoun:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field subject_pronoun", values[i])
			} else if value.Valid {
				_m.SubjectPronoun = value.String
			}
		case gender.FieldObjectPronoun:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field object_pronoun", values[i])
			} else if value.Valid {
				_m.ObjectPronoun = value.String
			}
		case gender.FieldPossessivePronoun:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field possessive_pronoun", values[i])
			} else if value.Valid {
				_m.PossessivePronoun = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Gender.
// This includes values selected through modifiers, order, etc.
func (_m *Gender) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this Gender.
// Note that you need to call Gender.Unwrap() before calling this method if this Gender
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Gender) Update() *GenderUpdateOne {
	return NewGenderClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Gender entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Gender) Unwrap() *Gender {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("db: Gender is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Gender) String() string {
	var builder strings.Builder
	builder.WriteString("Gender(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("display_name=")
	builder.WriteString(_m.DisplayName)
	builder.WriteString(", ")
	builder.WriteString("subject_pronoun=")
	builder.WriteString(_m.SubjectPronoun)
	builder.WriteString(", ")
	builder.WriteString("object_pronoun=")
	builder.WriteString(_m.ObjectPronoun)
	builder.WriteString(", ")
	builder.WriteString("possessive_pronoun=")
	builder.WriteString(_m.PossessivePronoun)
	builder.WriteByte(')')
	return builder.String()
}

// Genders is a parsable slice of Gender.
type Genders []*Gender
