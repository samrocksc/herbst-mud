// Code generated by ent, DO NOT EDIT.

package db

import (
	"fmt"
	"herbst-server/db/damagelog"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// DamageLog is the model entity for the DamageLog schema.
type DamageLog struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// ID of character who dealt damage
	AttackerID int `json:"attacker_id,omitempty"`
	// ID of character/NPC who took damage
	TargetID int `json:"target_id,omitempty"`
	// Damage amount
	Damage int `json:"damage,omitempty"`
	// When the damage was dealt
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DamageLog) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case damagelog.FieldID, damagelog.FieldAttackerID, damagelog.FieldTargetID, damagelog.FieldDamage:
			values[i] = new(sql.NullInt64)
		case damagelog.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DamageLog fields.
func (_m *DamageLog) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case damagelog.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case damagelog.FieldAttackerID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field attacker_id", values[i])
			} else if value.Valid {
				_m.AttackerID = int(value.Int64)
			}
		case damagelog.FieldTargetID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field target_id", values[i])
			} else if value.Valid {
				_m.TargetID = int(value.Int64)
			}
		case damagelog.FieldDamage:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field damage", values[i])
			} else if value.Valid {
				_m.Damage = int(value.Int64)
			}
		case damagelog.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DamageLog.
// This includes values selected through modifiers, order, etc.
func (_m *DamageLog) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this DamageLog.
// Note that you need to call DamageLog.Unwrap() before calling this method if this DamageLog
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *DamageLog) Update() *DamageLogUpdateOne {
	return NewDamageLogClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the DamageLog entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *DamageLog) Unwrap() *DamageLog {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("db: DamageLog is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *DamageLog) String() string {
	var builder strings.Builder
	builder.WriteString("DamageLog(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("attacker_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.AttackerID))
	builder.WriteString(", ")
	builder.WriteString("target_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.TargetID))
	builder.WriteString(", ")
	builder.WriteString("damage=")
	builder.WriteString(fmt.Sprintf("%v", _m.Damage))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// DamageLogs is a parsable slice of DamageLog.
type DamageLogs []*DamageLog
