// Code generated by ent, DO NOT EDIT.

package db

import (
	"fmt"
	"herbst-server/db/ability"
	"herbst-server/db/character"
	"herbst-server/db/characterability"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// CharacterAbility is the model entity for the CharacterAbility schema.
type CharacterAbility struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Ability slot 1-5
	Slot int `json:"slot,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CharacterAbilityQuery when eager-loading is set.
	Edges               CharacterAbilityEdges `json:"edges"`
	ability_characters  *int
	character_abilities *int
	selectValues        sql.SelectValues
}

// CharacterAbilityEdges holds the relations/edges for other nodes in the graph.
type CharacterAbilityEdges struct {
	// Character holds the value of the character edge.
	Character *Character `json:"character,omitempty"`
	// Ability holds the value of the ability edge.
	Ability *Ability `json:"ability,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// CharacterOrErr returns the Character value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CharacterAbilityEdges) CharacterOrErr() (*Character, error) {
	if e.Character != nil {
		return e.Character, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: character.Label}
	}
	return nil, &NotLoadedError{edge: "character"}
}

// AbilityOrErr returns the Ability value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CharacterAbilityEdges) AbilityOrErr() (*Ability, error) {
	if e.Ability != nil {
		return e.Ability, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: ability.Label}
	}
	return nil, &NotLoadedError{edge: "ability"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CharacterAbility) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case characterability.FieldID, characterability.FieldSlot:
			values[i] = new(sql.NullInt64)
		case characterability.ForeignKeys[0]: // ability_characters
			values[i] = new(sql.NullInt64)
		case characterability.ForeignKeys[1]: // character_abilities
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CharacterAbility fields.
func (_m *CharacterAbility) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case characterability.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case characterability.FieldSlot:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field slot", values[i])
			} else if value.Valid {
				_m.Slot = int(value.Int64)
			}
		case characterability.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field ability_characters", value)
			} else if value.Valid {
				_m.ability_characters = new(int)
				*_m.ability_characters = int(value.Int64)
			}
		case characterability.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field character_abilities", value)
			} else if value.Valid {
				_m.character_abilities = new(int)
				*_m.character_abilities = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the CharacterAbility.
// This includes values selected through modifiers, order, etc.
func (_m *CharacterAbility) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryCharacter queries the "character" edge of the CharacterAbility entity.
func (_m *CharacterAbility) QueryCharacter() *CharacterQuery {
	return NewCharacterAbilityClient(_m.config).QueryCharacter(_m)
}

// QueryAbility queries the "ability" edge of the CharacterAbility entity.
func (_m *CharacterAbility) QueryAbility() *AbilityQuery {
	return NewCharacterAbilityClient(_m.config).QueryAbility(_m)
}

// Update returns a builder for updating this CharacterAbility.
// Note that you need to call CharacterAbility.Unwrap() before calling this method if this CharacterAbility
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *CharacterAbility) Update() *CharacterAbilityUpdateOne {
	return NewCharacterAbilityClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the CharacterAbility entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *CharacterAbility) Unwrap() *CharacterAbility {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("db: CharacterAbility is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *CharacterAbility) String() string {
	var builder strings.Builder
	builder.WriteString("CharacterAbility(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("slot=")
	builder.WriteString(fmt.Sprintf("%v", _m.Slot))
	builder.WriteByte(')')
	return builder.String()
}

// CharacterAbilities is a parsable slice of CharacterAbility.
type CharacterAbilities []*CharacterAbility
