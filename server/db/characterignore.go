// Code generated by ent, DO NOT EDIT.

package db

import (
	"fmt"
	"herbst-server/db/character"
	"herbst-server/db/characterignore"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// CharacterIgnore is the model entity for the CharacterIgnore schema.
type CharacterIgnore struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// ID of the character being ignored
	IgnoredCharacterId int `json:"ignoredCharacterId,omitempty"`
	// IgnoredAt holds the value of the "ignoredAt" field.
	IgnoredAt time.Time `json:"ignoredAt,omitempty"`
	// Optional reason for ignoring
	Reason string `json:"reason,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CharacterIgnoreQuery when eager-loading is set.
	Edges              CharacterIgnoreEdges `json:"edges"`
	character_ignoring *int
	selectValues       sql.SelectValues
}

// CharacterIgnoreEdges holds the relations/edges for other nodes in the graph.
type CharacterIgnoreEdges struct {
	// Ignorer holds the value of the ignorer edge.
	Ignorer *Character `json:"ignorer,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// IgnorerOrErr returns the Ignorer value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CharacterIgnoreEdges) IgnorerOrErr() (*Character, error) {
	if e.Ignorer != nil {
		return e.Ignorer, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: character.Label}
	}
	return nil, &NotLoadedError{edge: "ignorer"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CharacterIgnore) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case characterignore.FieldID, characterignore.FieldIgnoredCharacterId:
			values[i] = new(sql.NullInt64)
		case characterignore.FieldReason:
			values[i] = new(sql.NullString)
		case characterignore.FieldIgnoredAt:
			values[i] = new(sql.NullTime)
		case characterignore.ForeignKeys[0]: // character_ignoring
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CharacterIgnore fields.
func (_m *CharacterIgnore) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case characterignore.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case characterignore.FieldIgnoredCharacterId:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field ignoredCharacterId", values[i])
			} else if value.Valid {
				_m.IgnoredCharacterId = int(value.Int64)
			}
		case characterignore.FieldIgnoredAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field ignoredAt", values[i])
			} else if value.Valid {
				_m.IgnoredAt = value.Time
			}
		case characterignore.FieldReason:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field reason", values[i])
			} else if value.Valid {
				_m.Reason = value.String
			}
		case characterignore.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field character_ignoring", value)
			} else if value.Valid {
				_m.character_ignoring = new(int)
				*_m.character_ignoring = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the CharacterIgnore.
// This includes values selected through modifiers, order, etc.
func (_m *CharacterIgnore) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryIgnorer queries the "ignorer" edge of the CharacterIgnore entity.
func (_m *CharacterIgnore) QueryIgnorer() *CharacterQuery {
	return NewCharacterIgnoreClient(_m.config).QueryIgnorer(_m)
}

// Update returns a builder for updating this CharacterIgnore.
// Note that you need to call CharacterIgnore.Unwrap() before calling this method if this CharacterIgnore
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *CharacterIgnore) Update() *CharacterIgnoreUpdateOne {
	return NewCharacterIgnoreClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the CharacterIgnore entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *CharacterIgnore) Unwrap() *CharacterIgnore {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("db: CharacterIgnore is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *CharacterIgnore) String() string {
	var builder strings.Builder
	builder.WriteString("CharacterIgnore(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("ignoredCharacterId=")
	builder.WriteString(fmt.Sprintf("%v", _m.IgnoredCharacterId))
	builder.WriteString(", ")
	builder.WriteString("ignoredAt=")
	builder.WriteString(_m.IgnoredAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("reason=")
	builder.WriteString(_m.Reason)
	builder.WriteByte(')')
	return builder.String()
}

// CharacterIgnores is a parsable slice of CharacterIgnore.
type CharacterIgnores []*CharacterIgnore
