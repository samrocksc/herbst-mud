// Code generated by ent, DO NOT EDIT.

package db

import (
	"fmt"
	"herbst-server/db/shopitem"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// ShopItem is the model entity for the ShopItem schema.
type ShopItem struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// FK to shop_template
	ShopID int `json:"shop_id,omitempty"`
	// FK to equipment_template for the item
	EquipmentTemplateID int `json:"equipment_template_id,omitempty"`
	// Item category for filtering (weapons, armor, potions, etc.)
	Category string `json:"category,omitempty"`
	// Base price in currency
	Price int `json:"price,omitempty"`
	// Current stock in shop inventory
	Quantity int `json:"quantity,omitempty"`
	// Maximum stack size for this item
	MaxStock int `json:"max_stock,omitempty"`
	// If false, item cannot be purchased
	IsEnabled bool `json:"is_enabled,omitempty"`
	// When this item was last restocked
	LastRestocked *time.Time `json:"last_restocked,omitempty"`
	selectValues  sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ShopItem) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case shopitem.FieldIsEnabled:
			values[i] = new(sql.NullBool)
		case shopitem.FieldID, shopitem.FieldShopID, shopitem.FieldEquipmentTemplateID, shopitem.FieldPrice, shopitem.FieldQuantity, shopitem.FieldMaxStock:
			values[i] = new(sql.NullInt64)
		case shopitem.FieldCategory:
			values[i] = new(sql.NullString)
		case shopitem.FieldLastRestocked:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ShopItem fields.
func (_m *ShopItem) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case shopitem.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case shopitem.FieldShopID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field shop_id", values[i])
			} else if value.Valid {
				_m.ShopID = int(value.Int64)
			}
		case shopitem.FieldEquipmentTemplateID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field equipment_template_id", values[i])
			} else if value.Valid {
				_m.EquipmentTemplateID = int(value.Int64)
			}
		case shopitem.FieldCategory:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field category", values[i])
			} else if value.Valid {
				_m.Category = value.String
			}
		case shopitem.FieldPrice:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field price", values[i])
			} else if value.Valid {
				_m.Price = int(value.Int64)
			}
		case shopitem.FieldQuantity:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field quantity", values[i])
			} else if value.Valid {
				_m.Quantity = int(value.Int64)
			}
		case shopitem.FieldMaxStock:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field max_stock", values[i])
			} else if value.Valid {
				_m.MaxStock = int(value.Int64)
			}
		case shopitem.FieldIsEnabled:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_enabled", values[i])
			} else if value.Valid {
				_m.IsEnabled = value.Bool
			}
		case shopitem.FieldLastRestocked:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_restocked", values[i])
			} else if value.Valid {
				_m.LastRestocked = new(time.Time)
				*_m.LastRestocked = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ShopItem.
// This includes values selected through modifiers, order, etc.
func (_m *ShopItem) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this ShopItem.
// Note that you need to call ShopItem.Unwrap() before calling this method if this ShopItem
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ShopItem) Update() *ShopItemUpdateOne {
	return NewShopItemClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ShopItem entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ShopItem) Unwrap() *ShopItem {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("db: ShopItem is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ShopItem) String() string {
	var builder strings.Builder
	builder.WriteString("ShopItem(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("shop_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ShopID))
	builder.WriteString(", ")
	builder.WriteString("equipment_template_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.EquipmentTemplateID))
	builder.WriteString(", ")
	builder.WriteString("category=")
	builder.WriteString(_m.Category)
	builder.WriteString(", ")
	builder.WriteString("price=")
	builder.WriteString(fmt.Sprintf("%v", _m.Price))
	builder.WriteString(", ")
	builder.WriteString("quantity=")
	builder.WriteString(fmt.Sprintf("%v", _m.Quantity))
	builder.WriteString(", ")
	builder.WriteString("max_stock=")
	builder.WriteString(fmt.Sprintf("%v", _m.MaxStock))
	builder.WriteString(", ")
	builder.WriteString("is_enabled=")
	builder.WriteString(fmt.Sprintf("%v", _m.IsEnabled))
	builder.WriteString(", ")
	if v := _m.LastRestocked; v != nil {
		builder.WriteString("last_restocked=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteByte(')')
	return builder.String()
}

// ShopItems is a parsable slice of ShopItem.
type ShopItems []*ShopItem
