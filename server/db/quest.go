// Code generated by ent, DO NOT EDIT.

package db

import (
	"encoding/json"
	"fmt"
	"herbst-server/db/quest"
	"herbst-server/db/schema"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Quest is the model entity for the Quest schema.
type Quest struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
	// Quest IDs that must be completed before this quest can be accepted
	PrerequisiteQuestIds []string `json:"prerequisite_quest_ids,omitempty"`
	// Ordered list of quest objectives
	Objectives []schema.QuestObjective `json:"objectives,omitempty"`
	// Rewards granted on quest completion
	Rewards schema.QuestRewards `json:"rewards,omitempty"`
	// Whether and how this quest can be repeated
	RepeatMode quest.RepeatMode `json:"repeat_mode,omitempty"`
	// Hours before re-accept allowed (only if repeat_mode=cooldown)
	CooldownHours int `json:"cooldown_hours,omitempty"`
	// Whether this quest can be accepted
	IsActive bool `json:"is_active,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the QuestQuery when eager-loading is set.
	Edges        QuestEdges `json:"edges"`
	selectValues sql.SelectValues
}

// QuestEdges holds the relations/edges for other nodes in the graph.
type QuestEdges struct {
	// Progress holds the value of the progress edge.
	Progress []*QuestProgress `json:"progress,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ProgressOrErr returns the Progress value or an error if the edge
// was not loaded in eager-loading.
func (e QuestEdges) ProgressOrErr() ([]*QuestProgress, error) {
	if e.loadedTypes[0] {
		return e.Progress, nil
	}
	return nil, &NotLoadedError{edge: "progress"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Quest) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case quest.FieldPrerequisiteQuestIds, quest.FieldObjectives, quest.FieldRewards:
			values[i] = new([]byte)
		case quest.FieldIsActive:
			values[i] = new(sql.NullBool)
		case quest.FieldID, quest.FieldCooldownHours:
			values[i] = new(sql.NullInt64)
		case quest.FieldName, quest.FieldDescription, quest.FieldRepeatMode:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Quest fields.
func (_m *Quest) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case quest.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case quest.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case quest.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				_m.Description = value.String
			}
		case quest.FieldPrerequisiteQuestIds:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field prerequisite_quest_ids", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.PrerequisiteQuestIds); err != nil {
					return fmt.Errorf("unmarshal field prerequisite_quest_ids: %w", err)
				}
			}
		case quest.FieldObjectives:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field objectives", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Objectives); err != nil {
					return fmt.Errorf("unmarshal field objectives: %w", err)
				}
			}
		case quest.FieldRewards:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field rewards", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Rewards); err != nil {
					return fmt.Errorf("unmarshal field rewards: %w", err)
				}
			}
		case quest.FieldRepeatMode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field repeat_mode", values[i])
			} else if value.Valid {
				_m.RepeatMode = quest.RepeatMode(value.String)
			}
		case quest.FieldCooldownHours:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field cooldown_hours", values[i])
			} else if value.Valid {
				_m.CooldownHours = int(value.Int64)
			}
		case quest.FieldIsActive:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_active", values[i])
			} else if value.Valid {
				_m.IsActive = value.Bool
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Quest.
// This includes values selected through modifiers, order, etc.
func (_m *Quest) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryProgress queries the "progress" edge of the Quest entity.
func (_m *Quest) QueryProgress() *QuestProgressQuery {
	return NewQuestClient(_m.config).QueryProgress(_m)
}

// Update returns a builder for updating this Quest.
// Note that you need to call Quest.Unwrap() before calling this method if this Quest
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Quest) Update() *QuestUpdateOne {
	return NewQuestClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Quest entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Quest) Unwrap() *Quest {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("db: Quest is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Quest) String() string {
	var builder strings.Builder
	builder.WriteString("Quest(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(_m.Description)
	builder.WriteString(", ")
	builder.WriteString("prerequisite_quest_ids=")
	builder.WriteString(fmt.Sprintf("%v", _m.PrerequisiteQuestIds))
	builder.WriteString(", ")
	builder.WriteString("objectives=")
	builder.WriteString(fmt.Sprintf("%v", _m.Objectives))
	builder.WriteString(", ")
	builder.WriteString("rewards=")
	builder.WriteString(fmt.Sprintf("%v", _m.Rewards))
	builder.WriteString(", ")
	builder.WriteString("repeat_mode=")
	builder.WriteString(fmt.Sprintf("%v", _m.RepeatMode))
	builder.WriteString(", ")
	builder.WriteString("cooldown_hours=")
	builder.WriteString(fmt.Sprintf("%v", _m.CooldownHours))
	builder.WriteString(", ")
	builder.WriteString("is_active=")
	builder.WriteString(fmt.Sprintf("%v", _m.IsActive))
	builder.WriteByte(')')
	return builder.String()
}

// Quests is a parsable slice of Quest.
type Quests []*Quest
