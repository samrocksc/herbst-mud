---
requires_bdd: false
---
We want to establish local development environment. 

1. create a makefile which will allow me to `make install` and install all dependencies in both the primary project and in admin project
2. Create a `make dev` command which will start the ssh server and the administrative backend.
